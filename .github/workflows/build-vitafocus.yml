name: Build VitaFocus APK

on:
  push:
    branches:
      - 'build/vitafocus-fixed-*'
    paths:
      - 'vitafocus-direct/**'
      - '.github/workflows/build-vitafocus.yml'
  pull_request:
    branches:
      - main
    paths:
      - 'vitafocus-direct/**'
      - '.github/workflows/build-vitafocus.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '8.10.2'
      - name: Build debug APK
        working-directory: vitafocus-direct
        run: gradle :app:assembleDebug --stacktrace
      - name: Prepare artifact
        run: |
          mkdir -p artifact
          cp vitafocus-direct/app/build/outputs/apk/debug/app-debug.apk artifact/VitaFocus-fixed-v1.1.0.apk
          sha256sum artifact/VitaFocus-fixed-v1.1.0.apk > artifact/VitaFocus-fixed-v1.1.0-SHA256.txt
      - uses: actions/upload-artifact@v4
        with:
          name: VitaFocus-fixed-v1.1.0
          path: artifact/*
          retention-days: 7
