# ─────────────────────────────────────────────────────────────
# REDIRECT HTTP → HTTPS PER TUTTI I DOMINI
# ─────────────────────────────────────────────────────────────
server {
    listen 80;
    server_name sgailegal.com sgailegal.it;
    return 301 https://$host$request_uri;
}

# ─────────────────────────────────────────────────────────────
# BLOCCO UNIFICATO HTTPS PER sgailegal.com E sgailegal.it
# ─────────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    server_name sgailegal.com sgailegal.it;
    root /ragflow/web/dist;

    # Certificati SSL (Let's Encrypt)
    ssl_certificate     /etc/letsencrypt/live/sgailegal.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/sgailegal.com/privkey.pem;

    # GZIP Compression
    gzip on;
    gzip_min_length 1k;
    gzip_comp_level 9;
    gzip_types text/plain application/javascript application/x-javascript text/css application/xml text/javascript application/x-httpd-php image/jpeg image/gif image/png;
    gzip_vary on;
    gzip_disable "MSIE\.";

    # Proxy per Flask (Ragflow backend)
    location ^~ /oauth/ {
    proxy_pass http://ragflow-backend-oauth:8000/;
    include /etc/nginx/proxy.conf;
}

    # Frontend React Single Page App
    location / {
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    # Static files long cache
    location ~ ^/static/(css|js|media)/ {
        expires 10y;
        access_log off;
    }
}
